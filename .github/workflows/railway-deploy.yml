name: CI/CD - Railway

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Run tests
        run: go test ./...

      - name: Set up Node.js (for Railway CLI)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Sync DATABASE_URL to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
          RAILWAY_ENVIRONMENT_ID: ${{ secrets.RAILWAY_ENVIRONMENT_ID }}
          DATABASE_URL_SECRET: ${{ secrets.RAILWAY_DATABASE_URL }}
        run: |
          set -euo pipefail
          if [ -z "${DATABASE_URL_SECRET:-}" ]; then
            echo "DATABASE_URL secret not provided; skipping sync."
            exit 0
          fi
          args=(variables set "DATABASE_URL=${DATABASE_URL_SECRET}")
          if [ -n "${RAILWAY_SERVICE_ID:-}" ]; then
            args+=(--service "${RAILWAY_SERVICE_ID}")
          fi
          if [ -n "${RAILWAY_ENVIRONMENT_ID:-}" ]; then
            args+=(--environment "${RAILWAY_ENVIRONMENT_ID}")
          fi
          railway "${args[@]}"

      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
          RAILWAY_ENVIRONMENT_ID: ${{ secrets.RAILWAY_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          args=(up)
          if [ -n "${RAILWAY_SERVICE_ID:-}" ]; then
            args+=(--service "${RAILWAY_SERVICE_ID}")
          fi
          if [ -n "${RAILWAY_ENVIRONMENT_ID:-}" ]; then
            args+=(--environment "${RAILWAY_ENVIRONMENT_ID}")
          fi
          railway "${args[@]}"
